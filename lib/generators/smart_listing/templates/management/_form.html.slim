td colspan="<%= columns.length + 1 %>"
  = simple_form_for(object, html: {class: 'form-horizontal' }, remote: true) do |f|
    .container
      .row
        = f.input :name
    .control-group
      .controls
        = f.submit t('shared.submit'), class: 'btn btn-primary'
        button.btn.btn-link.cancel Annuler

<%# 
  Show action.

  Available local variables:
    url
    icon
    title
-%>

<%= link_to(url, :class => "show", :title => title || t("smart_listing.actions.show") ) do %><%= content_tag(:i, '', :class => icon || SmartListing.config.classes(:icon_show)) %><% end %>

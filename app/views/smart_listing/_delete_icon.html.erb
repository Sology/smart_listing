<%= link_to( destroy_url, :remote => true, :class => "destroy", :method => :delete, :title => t("smart_listing.actions.destroy"), :data => {:confirmation => destroy_confirmation || t("smart_listing.msgs.destroy_confirmation")} ) do %><i class="<%= icon || "glyphicon glyphicon-trash" %>"></i><% end %>
